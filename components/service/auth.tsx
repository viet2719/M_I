async function authenticateUser(email: any, password: string) {
	try {
	} catch (error) {
		console.error('Đăng nhập thất bại:', error)
		// Xác thực thất bại, trả về false hoặc thông báo lỗi tùy theo yêu cầu của bạn
		return false
	}
}

export default authenticateUser
